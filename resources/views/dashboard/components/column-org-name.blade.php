<a href="javascript:void(0)" class="text-info text-decoration-none org-info" data-id="{{ $data->id }}"
    data-name="{{ $data->org_name }}" data-type="{{ $data->org_type }}"
    data-logo="{{ $data->org_image }}">{{ $data->org_name }}</a>
